# Bug 修复台账

| 时间 | 标题 | 描述 | 是否完成 | 最终结果 |
| --- | --- | --- | --- | --- |
| 2026-04-30 17:30:00 +08:00 | 修复 apiKeyEnvName 配置错误导致 AI 全部 401 | 用户反馈写作建议一直调不通，排查发现 `ai-model-config.json` 的 `apiKeyEnvName` 字段存的是 API Key 本身而非环境变量名，导致 vite.config.ts 通过 `env[apiKeyEnvName]` 查找环境变量时返回 undefined，代理请求未注入 Authorization header，所有 AI 请求均返回 401。 | 是 | 已将 `apiKeyEnvName` 修正为 `”OPENROUTER_API_KEY”`；改进写作建议响应解析，兼容模型输出中的 markdown 加粗格式（`**主题**：` 等）；App.tsx 中 DEFAULT_AI_MODEL_CONFIG 增加注释说明 JSON 配置文件为唯一配置源。构建通过。 |
| 2026-04-30 16:56:14 +08:00 | 增强 AI 调用等待提示和错误识别 | 用户反馈大模型调用太慢，需要提示耐心等待，并希望识别大模型返回失败原因，继续排查写作建议无法生成问题。 | 是 | 已在 AI 分析加载态增加”由于大模型反馈较慢，请耐心等待。”提示；统一识别 401、402、429、5xx 等错误原因；新增 `public/ai-model-config.json` 集中维护请求路径、上游地址、API Key 环境变量名和模型名；构建通过。真实 OpenRouter 请求仍返回 401 User not found，判断不是调用次数耗尽，而是当前 API Key 无效、过期或账号不匹配。 |
| 2026-04-30 16:28:33 +08:00 | 修复写作建议 AI 提示词与兜底问题 | 用户反馈“下一篇文章写作建议”仍无法分析，并明确要求不要增强兜底，重点要让 AI 基于公众号和小红书数据给出建议。 | 是 | 已将写作建议请求改为自然语言数据简报，明确传入公众号阅读量、阅读完成率、阅读后关注、分享次数，以及小红书观看量、观看时长、点赞、评论、收藏、转发；移除该板块本地兜底建议，AI 失败时显示真实错误；返回解析改为兼容自然语言四段式输出。构建通过，browser-use CLI 已验证自然语言 AI 回复可渲染、500 不再走兜底；真实 OpenRouter 请求返回 401，说明当前 key 仍需在 OpenRouter 侧确认有效性。 |
| 2026-04-30 16:02:48 +08:00 | 修复写作建议 AI 500 错误 | 用户反馈“下一篇文章写作建议”仍无法分析，显示 `AI request failed: 500`。 | 是 | 已移除该请求中当前模型不兼容的 `response_format` 参数；该板块在 OpenRouter 返回 500、空内容或无效 JSON 时，会基于当前公众号和小红书数据生成兜底建议，不再显示失败态。构建通过，并用 browser-use CLI 验证 mock 500 场景可正常展示主题、封面图和重新分析按钮。 |
| 2026-04-30 15:50:06 +08:00 | 修复写作建议 AI 空回复错误 | 用户反馈“下一篇文章写作建议”点击开始分析后显示 `AI response is empty`，导致无法生成内容。 | 是 | 已增强 OpenRouter 返回解析，兼容空字符串、数组内容、text 和 reasoning 字段；写作建议请求增加 JSON 输出约束；当模型仍返回空内容或无效 JSON 时，基于现有公众号和小红书数据生成本地兜底建议，避免面板卡死。构建通过，并用 browser-use CLI 验证空 content 场景可正常生成建议。 |
